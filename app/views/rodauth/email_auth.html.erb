<%= form_with url: rodauth.email_auth_path, method: :post, data: { turbo: false } do |form| %>
  <div class="mb-3 input-group-lg">
    <%= form.submit rodauth.login_button, class: "btn btn-lg btn-primary w-100" %>
  </div>
<% end %>
